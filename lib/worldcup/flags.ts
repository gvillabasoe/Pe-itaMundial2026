/**
 * Flag helpers — re-exports from normalize-team.
 * Use this as the public API for flag resolution in worldcup context.
 */

export { normalizeTeamKey, getFlagForTeam, getFlagEmoji } from "./normalize-team";
